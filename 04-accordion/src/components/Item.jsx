export default function Item({ title, text }) {
    return (
        <>
            <div>
                <span>{title}</span>
            </div>
            <div>
                <span>{text}</span>
            </div>
        </>
    );
}
